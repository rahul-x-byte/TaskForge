console.log('[Worker] TaskForge Worker service initialized.');
console.log('[Worker] Ready to process automation tasks with Playwright.');

// Keep process alive for dev mode
setInterval(() => {
  // heartbeat
}, 60000);
