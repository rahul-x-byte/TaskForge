{
  "name": "taskforge-monorepo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm run start:all",
    "start:all": "npx concurrently -n \"backend,frontend,worker\" -c \"cyan,magenta,yellow\" \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\" \"npm --prefix worker run dev\"",
    "build:all": "npm --prefix shared run build && npm --prefix backend run build && npm --prefix worker run build && npm --prefix frontend run build && npm --prefix extension run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
